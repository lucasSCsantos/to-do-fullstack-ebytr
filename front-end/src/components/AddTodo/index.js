import React from 'react'
import { Container, Modal } from './styled'

export default function index({ setOut }) {
	return (
		<Container onClick={setOut}>
			<Modal>

			</Modal>
		</Container>
	)
}
