import React from 'react'

export default function List({ todos }) {
	// console.log(todos)
	return (
		<div>
			{/* {todos.map(({title, description, author}) => (
				<div style={{ backgroundColor: 'blue'}}>
					<p>{title}</p>
					<p>{description && description}</p>
					<p>{author}</p>
				</div>
			))} */}
		</div>
	)
}
